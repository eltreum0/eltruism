local ElvUI_EltreumUI, E, L, V, P, G = unpack(select(2, ...))

-- Eltreum UI Private DB

V.ElvUI_EltreumUI = {

	install_version = nil,
	
	nameplateOptions = {
	ClassColorGlow = true,
	ClassBorderNameplate	= true,
	},
	
	stealthOptions = {
	stealtheffect = true,
	},
	
}
